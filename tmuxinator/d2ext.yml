# ~/.tmuxinator/d2ext.yml

project_name: d2ext
project_root: ~/php/extensions
tabs:
  - vim: cd ~/php/extensions && vim
  - shell: cd ~/php/extensions
  - tests: cd ~/php/extensions/tests
  - daemons: cd ~/php/extensions && guard # ruby guard daemon to generate ctags

